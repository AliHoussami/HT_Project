<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#888888"
    android:gravity="center">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Body Fat Calculator"
        android:textSize="24sp"
        android:textColor="#4CAF50"
        android:textStyle="bold"
        android:layout_marginBottom="24dp"/>

    <!-- Age Input -->
    <EditText
        android:id="@+id/etAge"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your age"
        android:inputType="number"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <!-- Height Input -->
    <EditText
        android:id="@+id/etHeight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter height in cm"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <!-- Weight Input -->
    <EditText
        android:id="@+id/etWeight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter weight in kg"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <EditText
        android:id="@+id/etNeck"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter neck circumference in cm"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <!-- Optional: Hip Circumference Input (for Women) -->
    <EditText
        android:id="@+id/etHip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter hip circumference in cm (for women)"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <!-- Waist Circumference Input -->
    <EditText
        android:id="@+id/etWaist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter waist circumference in cm"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"/>

    <!-- Gender Selection -->
    <RadioGroup
        android:id="@+id/radioGroupGender"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="24dp">

        <RadioButton
            android:id="@+id/radioMale"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Male"
            android:textColor="#FFFFFF"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:layout_marginEnd="16dp"/>

        <RadioButton
            android:id="@+id/radioFemale"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Female"
            android:textColor="#FFFFFF"
            android:minWidth="48dp"
            android:minHeight="48dp"/>
    </RadioGroup>

    <!-- Calculate Body Fat Button -->
    <Button
        android:id="@+id/btnCalculateBodyFat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calculate Body Fat"
        android:textColor="#FFFFFF"
        android:backgroundTint="#4CAF50"
        android:layout_marginBottom="24dp"/>

    <!-- Body Fat Result Display -->
    <TextView
        android:id="@+id/tvBodyFatResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Body Fat: "
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:layout_marginTop="16dp"/>
</LinearLayout>
